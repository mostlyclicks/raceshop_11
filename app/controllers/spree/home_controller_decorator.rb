class HomeController < Spree::StoreController

	# before_action :switch_to_brand

	# def switch_to_brand(brand)
	# 		if brand == "Rossignol"
	#   		redirect_to '/t/rossignol'
	#   	elsif brand == "Dynastar"
	#   		redirect_to '/t/dynastar'
	#   	elsif brand == "Coach"
	#   		redirect_to '/t/coach'
	#   	end
	# end

	# helper_method :switch_to_brand

end