
<% size ||= :mini %>
<% itemprop ||= nil %>

<% if image && image.attachment? %>
  <% if itemprop %>
    <%= image_tag image.attachment(size), itemprop: itemprop, class: 'img-responsive'  %>
  <% else %>
    <%= image_tag image.attachment(size), class: 'img-responsive' %>
  <% end %>
<% else %>
  <span class="image-placeholder <%= size %>"></span>
<% end %>




<% content_for(:image_2) do %>

<% if @flag == 'yes' %>
	<% size = :large %>
<% else %>
	<% size = :product %>
<% end %>

<%#= controller.action_name %>

<%# size ||= :large %>
<% itemprop ||= nil %>
<%#= @flag %>
<% if image && image.attachment? %>
  <% if itemprop %>
    <%= image_tag image.attachment(:product), itemprop: itemprop %>
  <% else %>
    <%= image_tag image.attachment(:large) %>
  <% end %>
<% else %>
  <span class="image-placeholder <%= size %>"></span>
<% end %>

<% end %>